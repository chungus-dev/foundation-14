# Scp added - isolated map for deterministic lighting benchmarks.
- type: gameMap
  id: ScpLightingBenchmark
  mapName: Lighting Benchmark
  mapPath: /Maps/_Scp/Test/lighting_benchmark.yml
  minPlayers: 0
  maxRandomOffset: 0
  randomRotation: false
  stations:
    ScpLightingBenchmark:
      stationProto: TestStation
      components:
      - type: StationNameSetup
        mapNameTemplate: Lighting Benchmark
      - type: StationJobs
        availableJobs:
          Passenger: [ -1, -1 ]
