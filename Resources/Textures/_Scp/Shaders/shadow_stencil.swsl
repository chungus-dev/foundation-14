light_mode unshaded;

void fragment()
{
    if (texture2D(TEXTURE, UV).a * MODULATE.a < 0.1)
        discard;

    // Transparent output keeps the light color intact while the shader instance updates stencil.
    COLOR = vec4(0.0);
}
